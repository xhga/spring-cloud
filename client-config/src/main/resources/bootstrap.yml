eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8767
spring:
  application:
    name: service-client-config
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8771/
